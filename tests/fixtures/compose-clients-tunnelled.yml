# Fixture for tests/compose-validation.bats (see the test that names this file).
name: fixture
services:
  gluetun:
    image: qmcgaw/gluetun:v3.41
    container_name: gluetun
  sabnzbd:
    image: lscr.io/linuxserver/sabnzbd:5.1.3
    container_name: sabnzbd
    network_mode: "service:gluetun"
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:5.1.2
    container_name: qbittorrent
    network_mode: "service:gluetun"
  # A client under a name nobody listed: only the image pattern can find it.
  dl:
    image: lscr.io/linuxserver/transmission:4.0.6
    container_name: dl
    network_mode: "service:gluetun"
  # Named after a client, but a sidecar — must NOT be pulled into the tunnel.
  sabnzbd-exporter:
    image: ghcr.io/onedr0p/sabnzbd-exporter:1.0.0
    container_name: sabnzbd-exporter
